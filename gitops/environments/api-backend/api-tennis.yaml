image:
  repository: image-registry.openshift-image-registry.svc:5000
  tag: latest

route:
  host: tennis-api.ocp.cloud.lab

build:
  git:
    contextDir: "api-tennis"

certificate:
  duration: 2880h
  renewBefore: 720h
  commonName: tennis-api.ocp.cloud.lab
  dnsNames:
    - tennis-api.ocp.cloud.lab
  issuerName: vault-cluster-issuer
  issuerKind: ClusterIssuer
  keyAlgorithm: RSA
  keySize: 2048