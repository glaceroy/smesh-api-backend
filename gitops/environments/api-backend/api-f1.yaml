image:
  repository: image-registry.openshift-image-registry.svc:5000
  tag: latest

route:
  host: formula1-api.ocp.cloud.lab

build:
  git:
    contextDir: "api-f1"

certificate:
  duration: 2880h
  renewBefore: 720h
  commonName: formula1-api.ocp.cloud.lab
  dnsNames:
    - formula1-api.ocp.cloud.lab
  issuerName: vault-cluster-issuer
  issuerKind: ClusterIssuer
  keyAlgorithm: RSA
  keySize: 2048